# Tasks

Use this file for durable project goals Marcus should keep in mind across sessions.

## Durable Goals

### Tooling Preference
- **Always use `uv`** for Python operations:
  - Run: `uv run`
  - Test: `uv run pytest` or `uv test`
  - Compile/Build: `uv build`
  - Never use `python`, `pip`, or `pytest` directly
