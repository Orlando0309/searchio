# User Preferences

## Tooling
- **Python runner**: `uv` (for run, test, compile)
- Always use `uv run`, `uv test`, or `uv build` instead of `python`, `pytest`, or `pip` directly.

## Notes
- This preference was set by the user and should be respected across all sessions.
