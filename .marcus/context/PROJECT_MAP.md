# Project Map
_Generated by Marcus from a bounded repo scan. Refreshes when repo layout or git status changes._
## Identity
- Project: searchio
- Git: master (dirty)
## Stack
- Python project (`pyproject.toml`)
- Marcus repo-local config (`.marcus/marcus.toml`)
## Folder structure
- `.marcus/` -> repo-local agents, flows, context, memory, and tasks
- `.pytest_cache/` -> contains v
- `core/` -> directory
- `gui/` -> directory
- `pyproject.toml` -> top-level project file
- `tests/` -> test suite
## Key files
- `.marcus/marcus.toml` -> repo-level Marcus config
- `pyproject.toml` -> Python project config
- `README.md` -> project overview
## Current work
- Active branch: `master` with uncommitted changes
- Recently changed files: `.marcus/context/.project_map_state.json`, `.marcus/context/PROJECT_MAP.md`, `.marcus/sessions/latest.json`, `.marcus/sessions/sess_1774645783.json`, `config.py`
- Changed areas: `.marcus`, `config.py`
## Patterns in use
- Repo-local workflows live under `.marcus/flows/`.